import { GitHubMentionsDirective } from './git-hub-mentions.directive';

describe('GitHubMentionsDirective', () => {
  it('should create an instance', () => {
    const directive = new GitHubMentionsDirective();
    expect(directive).toBeTruthy();
  });
});
